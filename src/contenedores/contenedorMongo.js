import {mongoose} from 'mongoose'
import { productosMongo } from '../../models/productos'



class ContenedorMongo{
    
   async listar(id){
      try{
         
        
      }catch(error){
          throw new Error(`Error al listar por id:${error}`)
      }
  }  













}